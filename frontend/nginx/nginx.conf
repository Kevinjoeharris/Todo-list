server {
    listen 3000;

    # Root directory for static files
    root /usr/share/nginx/html;

    # Default file to serve
    index index.html;

    location / {
        # This is the key line for SPAs: tries the file/directory, then falls back to index.html
        try_files $uri $uri/ /index.html;
    }
}